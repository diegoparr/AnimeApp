query GetAnimeMangasList($page : Int){
    Page(page: $page, perPage: 30) {
        media(sort: POPULARITY_DESC , isAdult: false) {
            id
            title {
                romaji
                english
                native

            }
            coverImage {
                large
            }
            type
            genres
            description
        }
    }
}